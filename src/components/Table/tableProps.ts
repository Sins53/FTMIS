export interface TableStyleProps {
  headerBgColor: string;
  headerColor: string;
  headerFontSize: string;
  headerFontWeight: string;
  headerPadding: string;
  bodyPadding: string;
  bodyFontSize: string;
  bodyColor: string;
  bodyBgColor: string;
  footerPadding: string;
  footerFontSize: string;
  footerColor: string;
  footerBgColor: string;
  borderColor: string;
}

export const tablePropertyStyles: TableStyleProps = {
  headerBgColor: '#2f6fcf',
  headerColor: '#ebebeb',
  headerFontSize: '0.875rem',
  headerFontWeight: '600',
  headerPadding: '0.5rem',
  bodyPadding: '0.5rem',
  bodyFontSize: '0.875rem',
  bodyColor: '#292929',
  footerPadding: '0.5rem',
  footerFontSize: '0.875rem',
  footerColor: '#292929',
  borderColor: '#f2f7ff',
  footerBgColor: '#d6e7ff',
  bodyBgColor: '#fffff'
};
