import { Box } from '@/components/core';
import styled from 'styled-components';

export const StyledProvinceBox = styled(Box)`
  width: 100%;
  padding-bottom: 1rem;
  flex-grow: 1;
  overflow-x: hidden;
`;
