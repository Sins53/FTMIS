import enWelcome from './welcome/en.json';
import npWelcome from './welcome/np.json';

const enPublic = {
  welcome: enWelcome
};
const npPublic = {
  welcome: npWelcome
};

const publicLang = { enPublic, npPublic };
export default publicLang;
